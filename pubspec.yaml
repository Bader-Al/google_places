name: google_places
description: A new Flutter package for handle google place api that place search and details and photos and autocomplete and query autocomplete requests are available
version: 0.4.8
homepage: https://github.com/Bader-Al/google_places.git

environment:
  sdk: ">=2.17.0 <3.0.0"


dependencies:
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
